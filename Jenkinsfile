pipeline{
    agent {
        dockerfile true
    }
    stages{
        stage ('Build') {
            steps {
                sh 'python3 --version'
            }
        }
    // hinh nhu jenkins auto clear cache + imagess cho
    }
}